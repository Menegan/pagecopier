services:
  - type: web
    name: pagecopier
    env: node
    buildCommand: npm install && PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=0 npx playwright install chromium
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
